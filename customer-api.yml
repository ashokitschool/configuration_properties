server:
  port: 7072
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ashokit_ecomm
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  boot:
   admin:
     client:
       url: http://localhost:1111/
  mail:
   host: smtp.gmail.com
   port: 587
   username: ashokit.classes@gmail.com
   password: 
   properties:
     mail:
      smtp:
        auth: true
        starttls:
        enable: true     
management:
  endpoints:
    web:
      exposure:
        include: '*'
